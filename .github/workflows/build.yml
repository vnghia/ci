name: GitHub CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:

  windows:
    name: Windows
    runs-on: windows-latest
    steps:
      - uses: actions/setup-python@v1
        with:
          python-version: 3.9
      - name: Windows
        shell: pwsh
        run: |
          docker run --rm busybox echo hello_world